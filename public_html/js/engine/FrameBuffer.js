LEEWGL.FrameBuffer = function (gl, options) {
    var handle = gl.glCreateFramebuffer();
    var width = options.width;
    var height = options.height;

    this.init = function () {
        

    };
};

